// Q71	Write a program to Binary search.

#include <stdio.h>

int main()
{
     int n,i,j;
    printf("Enter no of elements in array: ");
    scanf("%d",&n);

    int a[n];
    printf("Enter array elements: ");
    for ( i = 0; i < n; i++)
    {
        scanf("%d",&a[i]);
    }
    
    
    return 0;
}